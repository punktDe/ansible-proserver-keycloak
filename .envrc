#!/bin/sh
set -e

if [ ! -f venv-ansible-proserver-keycloak/bin/activate ]
then
	echo "Creating a virtual Python environment"
	python3 -m venv venv-ansible-proserver-keycloak >/dev/null
fi

if [ -f venv-ansible-proserver-keycloak/bin/activate ]
then
	echo "Activating the Python virtual environment"
	. venv-ansible-proserver-keycloak/bin/activate
fi

if [ -f venv-ansible-proserver-keycloak/bin/pip3 ]
then
	echo "Installing Python requirements"
	venv-ansible-proserver-keycloak/bin/pip3 install --upgrade pip 1> /dev/null
	venv-ansible-proserver-keycloak/bin/pip3 install -r requirements.txt 1> /dev/null
fi

